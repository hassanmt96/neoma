import React from 'react'
import { Field, reduxForm } from 'redux-form'

const TaskForm = (props) => {
    const {handleSubmit, rese}
    return (
        <div>
            
        </div>
    )
}


export default reduxForm({
    form: 'taskmaker'
})